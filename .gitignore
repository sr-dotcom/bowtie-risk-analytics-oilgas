# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Local configuration
.claude/

# Secrets
.env
.env.*
!.env.example
secrets/

# Data artifacts (never commit — reproduce via pipeline)
/data/*
!/data/README.md
!/data/sources/**
!/data/samples/**
!/data/evaluation/
!/data/evaluation/**
!/data/demo_scenarios/
!/data/demo_scenarios/**
!/data/rag/
!/data/rag/v2/**

# Deliverable packs (generated, shared out-of-band)
/out/

out/
data/processed/flattenedcsvs/*.xlsx

# Combined export artifacts
/data/processed/*combined*.csv
docs/plans/

# --- handover / exports / zips ---
HANDOVER_BUNDLE/
HANDOVER_BUNDLE.zip
bowtie_handover_bundle_*.zip
FETCH_HEAD
*.zip

# --- local work / quarantine / archive ---
_quarantine/
/archive/

# --- sensitive / local dev config ---
.mcp.json
skills-lock.json
.agents/
docs/handoff/v23_canonical_575/

# --- data (keep repo light) ---
data/raw/
data/processed/
data/text/
data/derived/
data/models/artifacts/
data/models/evaluation/
.gsd/
.gsd-id
.bg-shell/
.claude/skills/

# ── GSD baseline (auto-generated) ──
*~
*.code-workspace
node_modules/
.next/
*.pyc
target/
vendor/
*.log
coverage/
.cache/
tmp/

# --- frontend build cache ---
.vite/

# --- generated frontend demo scenarios (sourced from /data/demo_scenarios/) ---
frontend/public/demo-scenarios/*.json
!frontend/public/demo-scenarios/.gitkeep

# --- Windows alternate data streams ---
*:Zone.Identifier

# --- legacy source quarantine ---

# ── GSD baseline (auto-generated) ──
.gsd
data/exports/

# --- graphify ---
.graphifyignore
graphify-out/
.playwright-mcp/

# CCCLI session artifacts
SESSION_REPORT.md

# Sprint screenshots (working-tree noise; archive to /tmp/ before deletion)
/*.png

# Local docker dependency cache (torch wheel — not used by production Dockerfiles)
docker-deps/

# Playwright runtime output
frontend/test-results/

# M003 cascade model artifacts — required at runtime, baked into image
!/data/models/
!/data/models/artifacts/
!/data/models/artifacts/xgb_cascade_*.joblib
!/data/models/artifacts/xgb_cascade_*.json

# Cascading model training input — tracked per Patrick's OK to publish (S-005)
# Required by src/modeling/cascading/data_prep.py at data load time.
!/data/models/cascading_input/barrier_model_dataset_base_v3.csv

# Jeffrey's association-mining export — kept locally for analytical work;
# not consumed by current pipeline (data_prep.py uses v3 base file only)
data/models/cascading_input/barrier_threat_pairs_for_jeffrey_v2.csv

# Claude Code tooling state (local developer environment, not project content)
skills-lock.json

# handover-prep process artifacts (local only)
.handover/
